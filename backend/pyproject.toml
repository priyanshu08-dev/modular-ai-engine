[project]
name = "backend"
version = "0.1.0"
description = "Production-grade modular AI reasoning engine with RAG, LangChain, and LangGraph."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.138.1",
    "pydantic-settings>=2.14.2",
    "python-dotenv>=1.2.2",
    "uvicorn>=0.49.0",
]
